# Docker: setuptools 70.x for dlib build; cmake from system (apt), not pip
setuptools==70.3.0
wheel

Django>=4.2
djangorestframework
python-dotenv
dj-database-url
Pillow
django-cors-headers
djangorestframework-api-key
django-crispy-forms
crispy-bootstrap5
opencv-python-headless
numpy
gunicorn
daphne
channels
whitenoise
pdf2image
pytesseract
psycopg2-binary
# dlib builds from source on Linux (needs cmake + build-essential in Dockerfile)
# face-recognition depends on dlib
face-recognition
django-storages[s3]
boto3
requests

# Optional: celery/redis if you enable them
# celery
# redis
# django-celery-results
# django-celery-beat
